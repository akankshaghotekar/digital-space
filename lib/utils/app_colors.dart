import 'package:flutter/material.dart';

class AppColors {
  // Light
  static const lightBg = Color(0xFFF6F7FB);
  static const lightCard = Colors.white;
  static const lightText = Colors.black;
  static const lightSubText = Colors.grey;

  // Dark
  static const darkBg = Color(0xFF0E0E12);
  static const darkCard = Color(0xFF1C1C24);
  static const darkText = Colors.white;
  static const darkSubText = Colors.grey;

  // Common
  static const primaryBlue = Color(0xFF3B82F6);
  static const purple = Color(0xFF7C5DF7);
  static const blue = Color(0xFF38BDF8);
  static const green = Color(0xFF34D399);

  static get primaryColor => null;
}
